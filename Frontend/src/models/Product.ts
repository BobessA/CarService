export type Product = {
  productId: string;
  productType: string;
  name: string;
  brand: string;
  purchasePrice: number;
  sellingPrice: number;
  stockQuantity: number;
  description: string;
};